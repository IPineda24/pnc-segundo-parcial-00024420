spring:
  application:
    name: parcial02
  datasource:
    url: jdbc:postgresql://localhost:5432/parcial02
    username: postgres
    password: 2401
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true